name: Release action
# Call the reusable workflow release-solution-with-inputs.yml
# Release your solution to prod when you create a new release.

on:
  release:
      types: [created]        

jobs:
  Release-solution-DPCoEALMPOC1:
    uses: ./.github/workflows/release-solution-with-inputs.yml
    with:
      #You can specify the solution name here
      solution_name: DPCoEALMPOC1
      #Update your values here
      BUILD_ENVIRONMENT_URL: 'https://orgcaf2a17a.crm.dynamics.com'
      PRODUCTION_ENVIRONMENT_URL: 'https://orged2065ff.crm.dynamics.com'
      CLIENT_ID: '49740080-c68e-4ac2-b88f-3c2abedc05f9'
      TENANT_ID: '44ae661a-ece6-41aa-bc96-7c2c85a08941'      
    secrets:
      envSecret: ${{ secrets.PowerPlatformSPN }}
